!!!
%html
%head
  %title= title_meta
  = javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js'
  = javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/jquery-ui.min.js'
  = javascript_include_tag :all, :cache => true
  = stylesheet_link_tag %w(layout application forms maps), :cache => true

%body
  #header
    #app_title= link_to t(:'app.title'), root_url
    
    - if !page_title.blank?
      %h1= page_title

  #content
    - flash.each do |flash_type, message|
      %p{:class => 'flash ' + flash_type.to_s}= h(message)

    = yield
    
    - if yield :info
      #info= yield :info
  
  #navigation
    = render :partial => 'shared/navigation'
  #footer
    (C) 2009 by
    = link_to 'antga.me', root_url
    written by Peter Krenn and Thomas R. Koll during
    =link_to 'railsrumble 2009', 'http://r09.railsrumble.com'
    Read our
    = link_to 'blog', 'http://blog.antga.me'

  = render :partial => 'shared/google_analytics'