- @page_title = t(:'.page_title')

= error_messages_for :anthill
= render :partial => 'intro'

- form_for(:anthill, @anthill || current_user.anthills.new, anthills_url) do |form|
  = render :partial => 'form', :object => form
  
  %p.submit
    = submit_tag t(:'.submit')
